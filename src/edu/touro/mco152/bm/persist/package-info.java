/**
 * The persist package handles all matters related to persisting 
 * data objects (entities) to a permanent store.
 * It uses the Java persistence interface, and
 * typically is configured to use the Apache DERBY RDBMS
 * 
 */

package edu.touro.mco152.bm.persist;